# Knack — รายการฟีเจอร์ย่อยทั้งหมด

ไฟล์นี้ไล่มาจากโค้ดใน `Knack.swift` ทั้งไฟล์ ใช้สำหรับตัดสินใจว่าจะตัดอะไรออก
ติ๊ก `[x]` หน้าข้อที่จะตัด แล้วส่งไฟล์กลับมาได้เลย

รูปแบบแต่ละข้อ

- **ใช้ยังไง** — ท่าทาง ปุ่ม หรือทำงานเอง
- **ทำงานยังไง** — กลไกข้างในแบบสั้น ๆ
- **สวิตช์** — ค่า Config ที่คุม และค่าเริ่มต้นในวงเล็บ (`ไม่มี` = ตัดได้ด้วยการลบโค้ดเท่านั้น)

---

## 1. หน้าต่าง (W)

### ⬜ W1 · ลากชนขอบซ้าย/ขวา = ครึ่งจอ
- **ใช้ยังไง:** ลาก title bar ให้เคอร์เซอร์เข้าใกล้ขอบจอในระยะ 6px แล้วปล่อย
- **ทำงานยังไง:** ขึ้นกรอบพรีวิวสีฟ้า ปล่อยแล้วสั่งขนาดผ่าน Accessibility ถ้าแอปไม่ยอมรับขนาด (มี min size) จะวางกึ่งกลางโซนแทน
- **สวิตช์:** `snapEnabled` (เปิด) · ระยะขอบ `edgeSensitivity` (6px)

### ⬜ W2 · ลากชนขอบบน = เต็มจอ
- **ใช้ยังไง:** ลากชนขอบบนตรงกลาง ๆ จอ
- **ทำงานยังไง:** เต็มพื้นที่ใช้งาน ยังเห็น menu bar และ Dock ไม่ใช่ full screen ของ Mac
- **สวิตช์:** `snapEnabled` (เปิด)

### ⬜ W3 · ลากชน 4 มุม = 1/4 จอ
- **ใช้ยังไง:** ลากเข้ามุมในระยะ 150px จากมุม เข้าจากขอบข้างหรือขอบบนก็ได้
- **ทำงานยังไง:** มุมมีสิทธิ์เหนือขอบ ได้หน้าต่างขนาดครึ่งกว้าง × ครึ่งสูง
- **สวิตช์:** `cornerSize` (150px)

### ⬜ W4 · ลากชนขอบล่าง = ครึ่งล่าง
- **ใช้ยังไง:** ลากชนขอบล่าง
- **ทำงานยังไง:** ปิดไว้เพราะ Windows ไม่มีท่านี้
- **สวิตช์:** `bottomEdgeZone` (ปิด)

### ⬜ W5 · โหมดจอแนวตั้ง
- **ใช้ยังไง:** ทำงานเองเมื่อจอสูงกว่ากว้าง
- **ทำงานยังไง:** ขอบบน/ล่างกลายเป็นครึ่งบน/ครึ่งล่าง เต็มจอย้ายไปอยู่ช่องแรกของ Snap Layouts
- **สวิตช์:** `portraitTopBottom` (เปิด)

### ⬜ W6 · ช่องว่างระหว่างหน้าต่าง
- **ใช้ยังไง:** ตั้งค่าเป็นตัวเลข
- **ทำงานยังไง:** เว้นระยะรอบทุกโซนที่ snap
- **สวิตช์:** `gap` (0px) — ไม่มี UI ให้ตั้งแล้ว

### ⬜ W7 · แรงต้านขอบระหว่างสองจอ
- **ใช้ยังไง:** ทำงานเองเมื่อลากชนขอบที่ติดกับจออีกใบ
- **ทำงานยังไง:** รอ 0.28 วินาทีก่อนขึ้นพรีวิว ถ้าลากผ่านไปอีกจอเร็วกว่านั้นจะไม่ติด snap
- **สวิตช์:** `innerEdgeDwell` (0.28 วิ)

### ⬜ W8 · Snap Layouts
- **ใช้ยังไง:** เริ่มลากแล้วมีแถบเล็กโผล่กลางบนจอ เลื่อนเคอร์เซอร์ไปแตะแถบ
- **ทำงานยังไง:** แถบกางเป็น 6 เลย์เอาต์ (ultrawide ได้ 7 / จอแนวตั้งมีชุดของตัวเอง) ชี้ช่องย่อยแล้วปล่อยเมาส์ ลากออกห่างแถบจะยุบกลับ
- **สวิตช์:** `snapLayouts` (เปิด)

### ⬜ W9 · Snap Assist
- **ใช้ยังไง:** ทำงานเองหลัง snap เสร็จ
- **ทำงานยังไง:** ขึ้นพาเนลหน้าต่างอื่นสูงสุด 12 บานตรงช่องว่างที่เหลือ คลิกเลือกมาเติม ถ้าเป็นเลย์เอาต์จะถามต่อทีละช่องจนเต็ม ปิดด้วย Esc / คลิกที่อื่น / หมดเวลา 8 วินาที
- **สวิตช์:** `snapAssist` (เปิด)

### ⬜ W10 · เส้นแบ่งปรับขนาดคู่
- **ใช้ยังไง:** ชี้ตรงรอยต่อของหน้าต่างสองบานที่ขอบชิดกัน (ไม่ว่าจัดด้วย Knack ลากเอง หรือ tiling ของ macOS) แล้วลาก
- **ทำงานยังไง:** ทุก 2.5 วินาที อ่านหน้าต่างจริงบนจอจาก CGWindowList หาคู่ที่ขอบชิดกันบนจอเดียวกัน (ยอมช่องว่างถึง 14px และรักษาช่องว่างไว้ตอนลาก) รวมคู่ที่รอยต่อเดียวกันเป็นเส้นเดียว แล้วค่อยหา AXUIElement เฉพาะหน้าต่างที่มีรอยต่อ วางหน้าต่างล่องหนหนา 10px ไว้ตรงรอยต่อ ชี้แล้วเห็นแท่งขาว ลากแล้วสั่งเฟรมทั้งสองฝั่งพร้อมกัน (ตำแหน่ง → ขนาด → ตำแหน่ง กัน macOS ตัดขนาด) ระหว่างลากจะไม่จัดเส้นใหม่ ปล่อยเมาส์แล้วยิงตำแหน่งที่ปล่อยซ้ำให้สองฝั่งจบจุดเดียวกัน ขั้นต่ำกว้าง 220 / สูง 140
- **สวิตช์:** `dividers` (เปิด ตั้งพร้อม Windows Snap)

### ⬜ W11 · ลากออก = คืนขนาดเดิม
- **ใช้ยังไง:** ลากหน้าต่างที่ snap อยู่ห่างจากจุดกด 28px
- **ทำงานยังไง:** หดกลับขนาดเดิมโดยเคอร์เซอร์ยังเกาะ title bar ที่สัดส่วนเดิม ทำโดยยิง mouse up/down สังเคราะห์ให้แอปเริ่มลากใหม่
- **สวิตช์:** `unsnapOnDrag` (เปิด)

### ⬜ W12 · ลากขอบบน/ล่างชนขอบจอ = สูงเต็มจอ
- **ใช้ยังไง:** จับขอบหน้าต่างในระยะ 12px แล้วลากจนชนขอบจอ
- **ทำงานยังไง:** ความกว้างคงเดิม สูงเต็มจอ ลากกลับออกมา 30px คืนความสูงเดิม
- **สวิตช์:** `verticalMaximize` (เปิด)

### ⬜ W13 · ดับเบิลคลิกขอบบน/ล่างของหน้าต่าง = สูงเต็มจอ
- **ใช้ยังไง:** ดับเบิลคลิกที่ขอบ ไม่ต้องลาก
- **ทำงานยังไง:** เหมือน W12
- **สวิตช์:** `verticalMaximize` (เปิด)

### ⬜ W14 · ดับเบิลคลิก title bar = เต็มจอ / คืน
- **ใช้ยังไง:** ดับเบิลคลิก title bar
- **ทำงานยังไง:** หน่วง 0.18 วินาทีเพื่อทับพฤติกรรม zoom ของ Mac กดซ้ำคืนขนาดเดิม
- **สวิตช์:** `trueMaximize` (ปิด)

### ⬜ W15 · Aero Shake
- **ใช้ยังไง:** ระหว่างลาก สะบัดซ้ายขวา 3 ครั้ง (แต่ละครั้งเกิน 45px ภายใน 1.2 วินาที)
- **ทำงานยังไง:** ย่อหน้าต่างอื่นทั้งหมด เขย่าอีกครั้งคืนกลับ
- **สวิตช์:** `aeroShake` (ปิด)

### ⬜ W16 · Option+Tab สลับหน้าต่าง
- **ใช้ยังไง:** Option+Tab / Option+Shift+Tab (ปุ่มลัดระดับระบบตัวเดียวที่เหลืออยู่)
- **ทำงานยังไง:** ตารางหน้าต่างทุกบานเรียงตาม z-order สูงสุด 7 คอลัมน์ ปล่อย Option = เลือก ใช้ลูกศร / Esc / คลิกได้ หน้าต่างที่ย่อไว้แสดงจาง
- **สวิตช์:** `altTab` (เปิด) — ไม่มีสวิตช์ใน UI และดับตามเมื่อปิด Windows Snap

### ⬜ W17 · ภาพหน้าต่างจริง (Thumbnails)
- **ใช้ยังไง:** ทำงานเองใน W9 / W16 / W19
- **ทำงานยังไง:** ScreenCaptureKit จับภาพสูงสุด 14 บาน ย่อกว้าง 520px รวมหน้าต่างที่ย่อไว้และอยู่คนละ Space (หา window id จากรายการทุกหน้าต่างของระบบ) ถ้า ScreenCaptureKit จับไม่ได้จะดึงภาพสุดท้ายจาก window server ผ่าน CGWindowListCreateImage (เรียกผ่าน dlsym) และเก็บภาพล่าสุดไว้เสมอไม่ล้างทิ้ง เหมือนพรีวิวของ Windows ถ้าไม่ให้สิทธิ์ Screen Recording ทุกพาเนลใช้ไอคอนแอปแทนและยังทำงานได้ ต้อง macOS 14+
- **สวิตช์:** `windowPreviews` (เปิด)

### ⬜ W18 · ภาพขยับตามของจริง
- **ใช้ยังไง:** ทำงานเองระหว่างพาเนลเปิด
- **ทำงานยังไง:** จับภาพซ้ำทุก 0.4–0.7 วินาทีตามจำนวนหน้าต่าง
- **สวิตช์:** `livePreviews` (เปิด)

### ⬜ W19 · ชี้ไอคอน Dock แล้วเห็นหน้าต่าง
- **ใช้ยังไง:** ชี้ค้างที่ไอคอนแอปใน Dock 0.28 วินาที
- **ทำงานยังไง:** อ่านตำแหน่งไอคอนผ่าน Accessibility ของ Dock มี timer ทุก 0.12 วินาทีตลอดเวลา ขึ้นพาเนลหน้าต่างของแอปนั้น คลิกเพื่อเรียก คลิกที่อื่นปิด
- **สวิตช์:** `dockPeek` (เปิด)

### ⬜ W26 · Win+ลูกศร
- **ใช้ยังไง:** Win+← → ↑ ↓ เมื่อ Windows Shortcut Key เปิด
- **ทำงานยังไง:** กดซ้ำเดินสถานะแบบ Windows (← จากครึ่งขวาบน → ครึ่งซ้ายบน, ↓ จากครึ่งล่าง = คืนขนาดเดิม) หน้าต่างที่ไม่เคย snap แล้วกด ↓ จะย่อลงกลางจอ 60%×70%
- **สวิตช์:** ตาม Windows Shortcut Key

---

## 2. คีย์บอร์ด (K)

ทุกข้อในกลุ่มนี้ทำงานเฉพาะเมื่อ **Windows Shortcut Key เปิด** ยกเว้นที่ระบุ

### ⬜ K1 · Ctrl แทน Cmd
- **ใช้ยังไง:** Ctrl+ตัวอักษร A–Z (ยกเว้น H, M, Q, Y), 0–9, `=` `-` `/` และ Enter
- **ทำงานยังไง:** แก้ flag ของ event เดิมจาก Ctrl เป็น Cmd ข้ามในเทอร์มินัล 9 ตัว (Terminal, iTerm2, Warp, kitty, alacritty, Ghostty, Hyper, WezTerm) เพื่อให้ Ctrl+C ยังเป็น SIGINT
- **สวิตช์:** `keyboardStyleWindows` (เปิด)

### ⬜ K2 · Ctrl+← → = กระโดดทีละคำ
- **ทำงานยังไง:** แปลงเป็น Option+ลูกศร คง Shift ไว้ให้เลือกคำได้

### ⬜ K3 · Ctrl+Home / Ctrl+End = หัวท้ายเอกสาร
- **ทำงานยังไง:** แปลงเป็น Cmd+↑ / Cmd+↓

### ⬜ K4 · Ctrl+Backspace / Ctrl+Delete = ลบทีละคำ
- **ทำงานยังไง:** แปลงเป็น Option+Backspace / Option+Delete

### ⬜ K5 · Ctrl+Y = ทำซ้ำ (Redo)
- **ทำงานยังไง:** แปลงเป็น Cmd+Shift+Z

### ⬜ K6 · Home / End = ต้นท้ายบรรทัด
- **ทำงานยังไง:** แปลงเป็น Cmd+← / Cmd+→ คง Shift ไว้

### ⬜ K7 · Ctrl+Insert / Shift+Insert = คัดลอก / วาง
- **ทำงานยังไง:** ปุ่ม Insert ของ PC มาเป็น Help แปลงเป็น Cmd+C / Cmd+V

### ⬜ K8 · Ctrl+PgUp / PgDn = สลับแท็บ
- **ทำงานยังไง:** แปลงเป็น Ctrl+Shift+Tab / Ctrl+Tab
- **บั๊ก:** ลำดับเงื่อนไขผิด ทำให้กด PgDn เปล่า ๆ ก็ถูกแปลงเป็น Tab

### ⬜ K9 · Ctrl+Esc = เมนู Start
- **ทำงานยังไง:** ยิง Cmd+Space เปิด Spotlight (ซ้ำกับ Win+R)

### ⬜ K10 · Ctrl+Shift+Esc = Task Manager
- **ทำงานยังไง:** เปิด Task Manager ของ Knack (ดู S4)

### ⬜ K12 · ปุ่ม Win
| ปุ่ม | ทำอะไร | กลไก |
|---|---|---|
| Win+E | เปิด Finder ที่ home | NSWorkspace.open |
| Win+D | แสดงเดสก์ท็อป | ยิง Fn+F11 |
| Win+L | ล็อกจอ | ยิง Ctrl+Cmd+Q |
| Win+R | ช่องค้นหา | ยิง Cmd+Space |
| Win+Shift+S | ตัดภาพหน้าจอ | ยิง Cmd+Shift+4 |
| Win+Tab | Mission Control | ยิง Ctrl+↑ |
| Win+I | System Settings | เปิด URL |
| Win+. | อิโมจิ | ยิง Ctrl+Cmd+Space |
| Win+ลูกศร | จัดหน้าต่าง | ดู W26 |

- **ทำงานยังไง:** แยก Win จริงออกจาก Ctrl ที่แปลงมาโดยดูว่า event ไม่มี Ctrl ค้างอยู่

### ⬜ K13 · F-keys
| ปุ่ม | ทำอะไร |
|---|---|
| F5 | Cmd+R รีเฟรช (ไม่กันแอปไหนเลย) |
| Ctrl+F5 | Cmd+Shift+R รีเฟรชล้างแคช |
| F3 / Shift+F3 | Cmd+G / Cmd+Shift+G ค้นหาถัดไป/ก่อนหน้า |
| Ctrl+F4 | Cmd+W ปิดแท็บ |
| Option+F4 | Cmd+W ปิดหน้าต่าง (ไม่ใช่ปิดแอป) |
| F11 | Ctrl+Cmd+F เต็มจอของแอป |

### ⬜ K14 · PrtSc / Alt+PrtSc
- **ทำงานยังไง:** ปุ่ม F13 ยิง Cmd+Shift+3 จับทั้งจอ / Cmd+Shift+4 เลือกพื้นที่

### ⬜ K15 · Alt+← → นอก Finder = ถอย / เดินหน้า
- **ทำงานยังไง:** แปลงเป็น Cmd+[ / Cmd+]
- **ผลข้างเคียง:** ใช้ Option+ลูกศรเลื่อนทีละคำแบบ Mac ไม่ได้อีก

### ⬜ K16 · ปุ่มแบบ Explorer ใน Finder
| ปุ่ม | ทำอะไร |
|---|---|
| F2 | เปลี่ยนชื่อ |
| Enter | เปิดไฟล์ (Cmd+O) |
| Delete | ลงถังขยะ |
| Backspace | ลงถังขยะ (ค่า `finderBackspaceGoesUp` ให้ขึ้นโฟลเดอร์แทนยังมีในโค้ด แต่ไม่มี UI แล้ว) |
| Ctrl+X แล้ว Ctrl+V / Cmd+V | ย้ายไฟล์ (Cmd+Option+V) |
| Alt+← → | ถอย / เดินหน้า |

- **ทำงานยังไง:** ทุกปุ่มถาม Accessibility ก่อนว่ากำลังพิมพ์ชื่อไฟล์อยู่ไหม ค่า cutPending ไม่มี timeout ค้างข้ามเซสชันได้

### ⬜ K17 · Language Key Change (ปุ่มเปลี่ยนภาษา)
- **ใช้ยังไง:** แถว Language Key Change ในเมนูบาร์: สวิตช์เปิด/ปิด และลูกศรกางเลือกปุ่ม `` ` `` / Alt+Shift / Ctrl+Shift / Win+Space
- **ทำงานยังไง:** สลับ input source ตัวถัดไปที่เปิดไว้ ทำงานเมื่อ Windows Shortcut Key เปิด ปุ่ม `` ` `` ไม่กิน Shift+`` ` `` แล้ว จึงพิมพ์ `~` ได้ · Alt+Shift และ Ctrl+Shift ยิงตอนปล่อยปุ่ม ถ้ามีปุ่มอื่นคั่นจะไม่นับ · Win+Space ทับ Spotlight
- **สวิตช์:** `languageSwitchKey` (1 = ปุ่ม `)

### ⬜ K18 · Auto Language Switch (ระบบตรวจจับภาษา แก้คำพิมพ์ผิดแป้น)
- **ใช้ยังไง:** พิมพ์ "l;ylfu" ทั้งที่ตั้งใจพิมพ์ "สวัสดี"
- **ทำงานยังไง:** จำปุ่มที่พิมพ์ พอครบ 3 ตัวถาม NSSpellChecker ถ้าพิมพ์ไทยแต่แปลงเป็นอังกฤษแล้วเป็นคำจริง หรืออังกฤษที่ไม่มีทางเป็นคำแต่แปลงเป็นไทยแล้วสะกดถูก จะแก้ให้โดยยิง Backspace ทีละตัวแล้วส่ง unicode เข้าไปใหม่ พร้อมสลับภาษาแป้น
- **สวิตช์:** `layoutFix` (ปิด)
- ส่วนย่อย:
  - ⬜ **K18a** แก้อัตโนมัติตั้งแต่ตัวที่ 3 หรือรอจนเว้นวรรค/Enter — `layoutFixAuto` (เปิด)
  - ⬜ **K18b** ดักปุ่ม Enter ไว้แก้ก่อนแล้วยิง Enter คืนหลัง 0.06 วิ
  - ⬜ **K18c** ยกเลิกด้วยแตะ Shift 2 ครั้ง หรือ Esc — `layoutFixUndoKey` (0 = Shift×2)
  - ⬜ **K18d** ข้ามช่องรหัสผ่าน (อ่าน role และป้ายชื่อช่องผ่าน AX) และข้าม Xcode, VS Code, Cursor, IntelliJ, Sublime, เทอร์มินัล, 1Password, Keychain
  - ⬜ **K18e** อ่านตารางปุ่มจาก layout ที่ติดตั้งจริง รองรับปัตตะโชติ

### ⬜ K20 · Keyboard Clean
- **ใช้ยังไง:** สวิตช์ Keyboard Clean ในเมนู > Keyboard เปิด = ล็อก ปิด = ปลด (หรือปุ่ม Unlock บนป้ายลอย)
- **ทำงานยังไง:** InputTap กลืน keyDown/keyUp/flagsChanged และปุ่มสื่อ (NSSystemDefined) ทั้งหมด เมาส์ยังใช้ได้ มีป้ายลอยกลางจอบอกว่าล็อกอยู่ ปุ่ม Power/Touch ID ล็อกไม่ได้เพราะไม่ผ่าน event ไม่จำค่าข้ามการเปิดแอป
- **สวิตช์:** ไม่มีค่าเก็บ (สถานะในหน่วยความจำ)

### ⬜ K19 · Auto Keyboard Detect
- **ใช้ยังไง:** เปิดสวิตช์ ต้องสิทธิ์ Input Monitoring
- **ทำงานยังไง:** ฟัง HID ว่าปุ่มล่าสุดมาจากอุปกรณ์ไหน vendor Apple หรือชื่อมี "apple" = Mac นอกนั้น = PC ค่าจำแบบค้างจนกดจากอีกตัว ไม่ได้สิทธิ์ก็ใช้ Windows ไปก่อน
- **สวิตช์:** `perKeyboard` (ปิด)

---

## 3. เมาส์ (M)

### ⬜ M1 · Mouse Natural Scroll / Trackpad Natural Scroll
- **ใช้ยังไง:** 2 สวิตช์อิสระ เลือกทิศที่อยากได้ของแต่ละอุปกรณ์ ไม่ต้องสนค่าใน System Settings
- **ทำงานยังไง:** แยกล้อกับ trackpad ด้วย flag continuous ของ event เทียบค่าที่อยากได้กับ Natural scrolling ของระบบ ถ้าต่างกันจะสร้าง scroll event ใบใหม่ที่กลับทิศแล้วส่งแทน (บน macOS 26 แก้ค่าในที่ไม่ได้ผล) trackpad คัดลอก phase/momentum ให้ inertia ยังทำงาน ทำงานทุกโหมด
- **สวิตช์:** `mouseNaturalScroll` (ปิด = แบบ Windows) · `trackpadNaturalScroll` (เปิด = แบบ Mac)

### ⬜ M2 · ความเร็วล้อเมาส์
- **ทำงานยังไง:** คูณระยะเลื่อน 0.5–3 เท่า
- **สวิตช์:** `mouseScrollSpeed` (1.0) — ไม่มี UI แล้ว

### ⬜ M3 · Ctrl+ล้อเมาส์ = ซูม
- **ทำงานยังไง:** ยิง Cmd+= / Cmd+- เฉพาะล้อเมาส์ ข้ามเทอร์มินัล
- **สวิตช์:** ตาม Windows Shortcut Key

### ⬜ M4 · ปุ่มข้าง 4/5 = ถอย / เดินหน้า
- **ทำงานยังไง:** ยิง Cmd+[ / Cmd+] กลืนทั้งกดและปล่อย
- **สวิตช์:** `mouseSideButtons` (ปิด)

---

## 6. ระบบ (S)

### ⬜ S1 · เปิดตอนล็อกอิน
- **ทำงานยังไง:** SMAppService ต้อง macOS 13+
- **สวิตช์:** `launchAtLogin` (ปิด)

### ⬜ S3 · รอสิทธิ์อัตโนมัติ
- **ทำงานยังไง:** poll ทุก 1 วินาทีจนได้ Accessibility แล้วเริ่มเองไม่ต้อง relaunch + หน้าต่างขอ Screen Recording ที่มีปุ่ม relaunch
- **สวิตช์:** ไม่มี

### ⬜ S7 · System Monitor บนเมนูบาร์
- **ใช้ยังไง:** สวิตช์แยก 3 ตัว CPU / RAM / SSD ในเมนูบาร์ เปิดตัวไหนก็โชว์ตัวนั้น ปิดหมด = ซ่อนแถบ
- **ทำงานยังไง:** status item ตัวที่สองข้างไอคอน Knack เช่น `CPU 12%  RAM 63%  SSD 45%` อัปเดตทุก 2 วินาที CPU จาก host_statistics (delta ของ ticks) RAM แบบ Activity Monitor (app + wired + compressed) SSD = % เวลาที่ดิสก์ยุ่งอ่าน/เขียน (สถิติ IOBlockStorageDriver แบบ iostat เอาไดรฟ์ที่ยุ่งสุด) ค่าเกิน 70/85% เป็นสีส้ม/แดง คลิกแล้วเห็นรายละเอียด (GB, ความเร็วอ่าน/เขียน, ความจุ) พร้อมปุ่มเปิด Activity Monitor และซ่อนทั้งหมด
- **สวิตช์:** `monitorCPU` · `monitorRAM` · `monitorSSD` (เปิดทั้งหมด)

### ⬜ S8 · Donate (แจกฟรี รับบริจาค)
- **ใช้ยังไง:** รายการ Donate… ในเมนู > System
- **ทำงานยังไง:** หน้าต่างแสดง QR PromptPay ที่สร้างสดจาก payload ใน `Sales.promptPayPayload` (ถอดจาก QR ของธนาคาร คมทุกขนาด) ชื่อบัญชี และปุ่ม GitHub Sponsors (`Sales.sponsorsURL`) ไม่มีระบบ license หรือช่วงทดลองแล้ว ทุกฟีเจอร์ใช้ได้เต็มตลอด
- **สวิตช์:** ไม่มี

### ⬜ S9 · หน้าขอสิทธิ์ (PermissionsWindow)
- **ใช้ยังไง:** เปิดเองตอนติดตั้งครั้งแรกและตอน Live Preview ยังไม่มี Screen Recording หรือรายการ Permissions… ในเมนูเมื่อยังขาด
- **ทำงานยังไง:** 3 แถว Accessibility / Screen Recording / Input Monitoring สถานะสด ปุ่ม "Grant all…" ขอสิทธิ์แรกที่ยังขาด (เด้งหน้าต่างของระบบ + เปิด System Settings หมวดนั้น) พอผู้ใช้ติ๊กเสร็จ Knack ตรวจเจอเองแล้วขออันถัดไปทันที ไม่ต้องกลับมากดทีละอัน มีข้อความบอกขั้นที่ x จาก y และปุ่ม Relaunch เมื่อจำเป็น (Screen Recording)

### ⬜ S10 · Check for Updates
- **ทำงานยังไง:** อ่าน JSON จาก `Sales.updateFeed` ตอนเปิดแอปและทุก 24 ชม. เทียบเลข build เสนอ Download / Later / Skip

---

## 7. จอภาพ (D)

### ⬜ D1 · รายการจอ + ปิดจอจริง
- **ใช้ยังไง:** สวิตช์บนแถวจอในเมนูบาร์
- **ทำงานยังไง:** ใช้ private API `CGSConfigureDisplayEnabled` (ตัวเดียวกับ BetterDisplay) ปฏิเสธถ้าเหลือจอเดียว จอที่ปิดหายจากระบบ Knack จำเองใน UserDefaults เพื่อโชว์สวิตช์เปิดกลับ และจำเป็น "ความต้องการ": รีสตาร์ทหรือเสียบจอแล้ว macOS เปิดจอนั้นคืนมา Knack จะปิดให้ซ้ำอัตโนมัติ (รอ 2–2.5 วิให้ระบบจัดจอเสร็จ และไม่ดับจอสุดท้าย) เทียบจอด้วย id / ชนิด built-in / ชื่อ เพราะ id เปลี่ยนได้หลังรีสตาร์ท กดสวิตช์ปิด/เปิดจอแล้วเมนูจะปิดก่อนและเปิดใหม่ใต้ menu bar ของจอที่เหลือ
- **สวิตช์:** ไม่มี

### ⬜ D2 · เปลี่ยนความละเอียด / Hz
- **ทำงานยังไง:** CGDisplay API เลือกความละเอียดแล้วใช้ Hz สูงสุดที่รองรับ
- **สวิตช์:** ไม่มี

### ⬜ D3 · หมุนจอ
- **ทำงานยังไง:** โหลด MonitorPanel.framework (private) แล้วเรียก KVC `setOrientation` ถ้า macOS เปลี่ยนโครงสร้างจะหายเงียบ ๆ
- **สวิตช์:** ไม่มี

### ⬜ D4 · ตำแหน่งจอ / ตั้งเป็นจอหลัก
- **ทำงานยังไง:** `CGConfigureDisplayOrigin`
- **สวิตช์:** ไม่มี

### ⬜ D5 · แยกจอที่ clone กัน
- **ทำงานยังไง:** ยกเลิก mirror ทุกจอ
- **สวิตช์:** ไม่มี

---

## 8. หน้าตาเมนู (UI)

### ⬜ U1 · เมนูกระจก Liquid Glass (MenuPanel)
- **ใช้ยังไง:** คลิกไอคอน Knack บนเมนูบาร์ เมนูเด้งลงมาใต้ไอคอน ปิดด้วยการคลิกที่อื่น กด Esc หรือคลิกไอคอนซ้ำ
- **ทำงานยังไง:** ไม่ใช่ NSMenu แล้ว เป็น NSPanel ที่ครอบด้วย NSGlassEffectView สไตล์ clear (macOS 26) มุมโค้ง 22 ทุกแถวเป็น view ที่วาดเอง (สวิตช์ หัวข้อ เส้นคั่น รายการติ๊กถูก แถวจอ) สับสวิตช์แล้ววาดใหม่ในที่โดยเมนูไม่ปิด ตัวเลือกของจอ (ความละเอียด/Hz/หมุน/ตำแหน่ง) กางรายการในที่แทนเมนูย่อย
- **สวิตช์:** ไม่มี

---

## ข้อสังเกตเพิ่มเติม

**โค้ดตาย** ลบหมดแล้ว (5 ก.ย. 2026) พร้อมหน้าตั้งค่า ระบบสองภาษา และฟีเจอร์ที่เข้าถึงได้เฉพาะจากหน้าตั้งค่า

**บั๊กที่ควรแก้ไม่ว่าจะตัดอะไร**
- K8 — PgDn เปล่า ๆ ถูกแปลงเป็น Tab

**ตัวที่กิน CPU เบื้องหลังตลอดเวลา**
| ฟีเจอร์ | ความถี่ |
|---|---|
| W19 Dock peek | ทุก 0.12 วิ |
| W10 เส้นแบ่ง | ทุก 2.5 วิ |

**อื่น ๆ**
- event tap คีย์บอร์ดเปิดตลอดโดยปริยาย เพราะ `keyboardStyleWindows` เป็น true ตั้งแต่ต้น
- ถ้าตัด W17 (Thumbnails) ทิ้งทั้งก้อน แอปจะไม่ต้องขอสิทธิ์ Screen Recording อีกเลย และทุกพาเนลยังใช้ได้ด้วยไอคอน
